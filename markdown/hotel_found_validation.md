# Goal

1. frontend shouldn't send the request for active hotel.
2. if we can send backend shouldn't be making hotels (validation at backend)
3. backend should return 400 bad request.
4. notify the user that hotel is already active.

# plan :

- [ ] store the hotel status.
- [ ] early exit if it is.
